# Evidence and claim boundaries

This scaffold is based on the supplied NTU course presentation.

## Supported by the presentation

- computer vision pipeline using grayscale/privacy masking, band-pass thresholding and connected-component centroid extraction;
- conversion from pixel displacement to physical velocity;
- iPad Air 5 example scale: 22.6 cm / 2360 px = 0.00958 cm/pixel;
- 12-point experiment matrix: 3 gestures × 4 sensitivity configurations;
- nonlinear least-squares fitting with SciPy;
- reported R² = 0.97;
- reported iPad Air 5 values `K_sys = 1`, `K_accel = 0.003`;
- manifest-based agent skill concept with price 0.01 USDC;
- escrow payment flow shown as an architecture design.

## Not currently reproducible from the supplied material

- the raw 12-point dataset;
- per-frame CV traces;
- the original source implementation;
- a deployed escrow contract / testnet transaction hash.

For a graduate-application portfolio, keep the reported numerical results, but label them as course-project results until the underlying data is checked into the repository.
